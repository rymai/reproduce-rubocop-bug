RSpec.describe Rubocop::Inheritance::Bug::Gem do
  it "has a version number" do
    expect(Rubocop::Inheritance::Bug::Gem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
